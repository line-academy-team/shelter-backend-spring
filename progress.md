### 1. 쉼터를 화면에 출력하기 위한 정보 수집
- 전국 단위 쉼터 API는 존재하지 않음
- 서울시 공공데이터 사이트 (https://data.seoul.go.kr/)에서 각 쉼터 정보 제공 중

### 2. 서울시 공공데이터 사이트에서 제공하는 API 현황
- 무더위쉼터
- 한파쉼터
- 기후동행쉼터
- 스마트쉼터 (API X, 파일만 제공)
- 도서관쉼터 (API X, 파일만 제공)
- 미세먼지쉼터

### 3. 문제 봉착
- 무더위쉼터,한파쉼터 API는 경도와 위도를 제공 중
- 기후동행쉼터, 미세먼지쉼터는 경도 위도로 절대 값을 제공하는게 아니라 서울시 중부원점을 기준으로 한 상대 TM좌표만 제공 중

### 4. 문제 해결
- 기후동행쉼터, 미세먼지쉼터의 상대 좌표를 데이터베이스에 저장할 때 라이브러리를 통한 절대좌표로 변환하여 저장하기로 함
- proj4j 라이브러리 (좌표변환 라이브러리) 이용

### 5. 북마크 기능 계획
- 맵에서 북마크한 쉼터만 조회할 수 있는 기능 (지도 표현) 을 생각했으나 메뉴 2개가 지도로 표현되기에 겹치는 느낌 존재
- 맵 필터링에 북마크만 볼 수 있는 기능으로 넣게 되면 메뉴가 줄어듬
- 즐겨찾기 목록으로 메뉴를 따로 빼도록 고안
